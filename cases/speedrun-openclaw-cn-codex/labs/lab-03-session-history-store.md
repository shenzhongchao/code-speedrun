# Lab 03: Session History Store

目标：理解 session history 与 memory recall 的区别。

任务：把 Unit 4 的 in-memory session store 接到 Unit 3 的 workspace store fixture。

验收：第二个 run 的 transcript 包含上一个 run 的 assistant 回复。
